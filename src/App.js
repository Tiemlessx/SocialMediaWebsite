import Home from "./pages/home/Home";

function App() {
  return <Home />;
}

export default App;

// make general color and add it to everything
// make darkmode color of ^ and replace it when swap to darkmode
